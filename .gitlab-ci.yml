# .gitlab-ci.yml
stages:
  - lint
  - test

pylint:
  stage: lint
  tags:
    - linux
  before_script:
    - pip install pylint
  script:
    - pylint ./src
  artifacts:
    when: always
    paths:
      - pylint.log

black:
  stage: lint
  before_script:
    - pip install black
  tags:
    - linux
  script:
    - black --check .
  artifacts:
    when: always
    paths:
      - black.log

test:
  stage: test
  before_script:
    - pip install -r requirements.txt
  tags:
    - linux
  script:
    - python -m unittest discover
  artifacts:
    when: always
    paths:
      - test-results.xml
